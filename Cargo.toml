[package]
name = "cni"
version = "0.4.0"
authors = ["Johann150 <johann+cni@qwertqwefsday.eu>"]
edition = "2018"
description = "parser for the CNI configuration format"
readme = "README.md"
repository = "https://github.com/Johann150/cni"
license = "Unlicense"
keywords = ["configuration", "ini", "cni"]
categories = ["config", "encoding", "parser-implementations"]
exclude = ["src/tests/**"]

[dependencies]

[dev-dependencies]
serde_json = "1.0"

[features]
default = ["ini"]
# enables the INI compatibility `ini`
ini = []
# enables ext `more-keys`
# this has been accepted but not yet merged (libuconf/cni/#2)
more-keys = []
